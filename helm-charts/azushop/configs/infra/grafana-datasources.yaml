apiVersion: 1

datasources:
  - name: ClickHouse
    type: grafana-clickhouse-datasource
    access: proxy
    url: http://{{ .Values.clickhouse.service.name }}:{{ .Values.clickhouse.service.httpPort }}
    isDefault: true
    jsonData:
      defaultDatabase: {{ .Values.clickhouse.auth.database | quote }}
