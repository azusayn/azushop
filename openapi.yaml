# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: ProductService API
    description: api for both merchants and customers.
    version: 0.0.1
paths:
    /v1/products:
        post:
            tags:
                - ProductService
            description: creates new products for given information.
            operationId: ProductService_BatchCreateProduct
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/BatchCreateProductRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/BatchCreateProductResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        patch:
            tags:
                - ProductService
            description: updates products' fields specified by the given update masks.
            operationId: ProductService_BatchUpdateProduct
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/BatchUpdateProductRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/BatchUpdateProductResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/sellers/{sellerId}/products:
        get:
            tags:
                - ProductService
            description: retrieves product information for given seller_id and product status.
            operationId: ProductService_ListSellerProducts
            parameters:
                - name: sellerId
                  in: path
                  required: true
                  schema:
                    type: integer
                    format: int32
                - name: pageToken
                  in: query
                  description: use 0 for the first request.
                  schema:
                    type: string
                - name: pageSize
                  in: query
                  description: max 100
                  schema:
                    type: integer
                    format: int32
                - name: productStatus
                  in: query
                  description: filter by product status.
                  schema:
                    type: integer
                    format: enum
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListSellerProductsResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/skus/batch:
        post:
            tags:
                - ProductService
            description: retrieves sku information for given skuIds.
            operationId: ProductService_BatchGetSkus
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/BatchGetSkusRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/BatchGetSkusResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        BatchCreateProductRequest:
            type: object
            properties:
                products:
                    type: array
                    items:
                        $ref: '#/components/schemas/Product'
        BatchCreateProductResponse:
            type: object
            properties: {}
        BatchGetSkusRequest:
            type: object
            properties:
                pageToken:
                    type: string
                    description: use "" for the first request.
                pageSize:
                    type: integer
                    description: max 100
                    format: int32
                skuIds:
                    type: array
                    items:
                        type: string
        BatchGetSkusResponse:
            type: object
            properties:
                skuDetails:
                    type: array
                    items:
                        $ref: '#/components/schemas/SkuDetail'
                nextPageToken:
                    type: string
        BatchUpdateProductRequest:
            type: object
            properties:
                products:
                    type: array
                    items:
                        $ref: '#/components/schemas/Product'
                updateMask:
                    type: string
                    format: field-mask
        BatchUpdateProductResponse:
            type: object
            properties: {}
            description: 'TODO(2): update strategy and update results.'
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        ListSellerProductsResponse:
            type: object
            properties:
                products:
                    type: array
                    items:
                        $ref: '#/components/schemas/Product'
        Product:
            type: object
            properties:
                id:
                    type: string
                    description: 'TODO(3): optional when used as input'
                productName:
                    type: string
                sellerId:
                    type: integer
                    description: 'TODO(3): optional when used as input'
                    format: int32
                skus:
                    type: array
                    items:
                        $ref: '#/components/schemas/Sku'
                productStatus:
                    type: integer
                    format: enum
        Sku:
            type: object
            properties:
                id:
                    type: string
                    description: 'TODO(3): optional when used as input'
                attrs:
                    type: object
                unitPrice:
                    type: string
        SkuDetail:
            type: object
            properties:
                sku:
                    $ref: '#/components/schemas/Sku'
                productName:
                    type: string
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: ProductService
