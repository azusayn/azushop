# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: ProductService API
    description: api for both merchants and customers.
    version: 0.0.1
paths:
    /v1/merchant/products:
        get:
            tags:
                - ProductService
            description: list all the products of the merchant.
            operationId: ProductService_ListMerchantProducts
            parameters:
                - name: pageToken
                  in: query
                  description: use 0 for the first request.
                  schema:
                    type: string
                - name: pageSize
                  in: query
                  description: max 100
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListMerchantProductsResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/products/{id}:
        patch:
            tags:
                - ProductService
            description: insert or update a product's details.
            operationId: ProductService_UpsertProduct
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpsertProductRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpsertProductResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/sellers/{sellerId}/products:
        get:
            tags:
                - ProductService
            description: list products by seller_id, generally used by customers.
            operationId: ProductService_ListProductsBySellerId
            parameters:
                - name: sellerId
                  in: path
                  required: true
                  schema:
                    type: integer
                    format: int32
                - name: pageToken
                  in: query
                  description: use 0 for the first request.
                  schema:
                    type: string
                - name: pageSize
                  in: query
                  description: max 100
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListProductsBySellerIdResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        ListMerchantProductsResponse:
            type: object
            properties:
                products:
                    type: array
                    items:
                        $ref: '#/components/schemas/MerchantProduct'
        ListProductsBySellerIdResponse:
            type: object
            properties:
                products:
                    type: array
                    items:
                        $ref: '#/components/schemas/Product'
        MerchantProduct:
            type: object
            properties:
                id:
                    type: string
                productName:
                    type: string
                sellerId:
                    type: integer
                    description: 'TODO: just extract it from token.'
                    format: int32
                skus:
                    type: array
                    items:
                        $ref: '#/components/schemas/MerchantSku'
        MerchantSku:
            type: object
            properties:
                skuDetail:
                    $ref: '#/components/schemas/SkuDetail'
                stockQuantity:
                    type: string
                reservedQuantity:
                    type: string
            description: sku message only used by merchant.
        Product:
            type: object
            properties:
                id:
                    type: string
                productName:
                    type: string
                sellerId:
                    type: integer
                    description: 'TODO: just extract it from token.'
                    format: int32
                skus:
                    type: array
                    items:
                        $ref: '#/components/schemas/Sku'
        Sku:
            type: object
            properties:
                skuDetail:
                    $ref: '#/components/schemas/SkuDetail'
                availableQuantity:
                    type: string
        SkuDetail:
            type: object
            properties:
                id:
                    type: string
                attrs:
                    type: object
                unitPrice:
                    type: string
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        UpsertProductRequest:
            type: object
            properties:
                id:
                    type: string
                productName:
                    type: string
                sellerId:
                    type: integer
                    description: 'TODO: just extract seller_id from token.'
                    format: int32
                skuDetails:
                    type: array
                    items:
                        $ref: '#/components/schemas/SkuDetail'
                updateMask:
                    type: string
                    format: field-mask
        UpsertProductResponse:
            type: object
            properties: {}
tags:
    - name: ProductService
